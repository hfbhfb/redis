{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "make",
            "args": ["CFLAG=\"-g -o0\""],
            "options": {
                "cwd":"${fileDirname}"
            }
        }
    ]
}